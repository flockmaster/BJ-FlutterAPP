# Skills 目录说明

这里是存储 AI 技能（Skills）的地方。每个技能应该是一个独立的子文件夹，并包含以下内容：

- `SKILL.md`: 核心指令文件，包含技能名称、描述和详细指令。
- `scripts/`: (可选) 存放该技能相关的辅助脚本。
- `examples/`: (可选) 存放示例代码或参考实现。
- `resources/`: (可选) 存放其他资源文件。

## 如何使用
当我检测到相关的任务时，我会根据约定读取对应的 `SKILL.md` 并遵循其指令。
